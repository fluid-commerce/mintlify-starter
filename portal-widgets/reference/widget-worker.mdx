---
title: "Widget Worker SDK Reference"
description: "Generated reference for the supported Fluid Widget Worker SDK facade."
---

The generated pages in this section document the supported public worker entry point:

```ts
import {
  defineWidget,
  defineWidgetPackage,
  startWidgetPackage,
} from "@fluid-app/portal-sdk/widgets/worker";
```

Use the reference for exact signatures, inputs, results, errors, property fields, portal functions, declared capabilities, and worker-safe elements.

Start with the primary authoring APIs:

- [`defineWidget`](/portal-widgets/reference/widget-worker-sdk/functions/defineWidget)
- [`defineWidgetPackage`](/portal-widgets/reference/widget-worker-sdk/functions/defineWidgetPackage)
- [`startWidgetPackage`](/portal-widgets/reference/widget-worker-sdk/functions/startWidgetPackage)
- [`DefineWidgetOptions`](/portal-widgets/reference/widget-worker-sdk/interfaces/DefineWidgetOptions)
- [`PortalFunctionError`](/portal-widgets/reference/widget-worker-sdk/classes/PortalFunctionError)

Worker-safe Fluid UI:

- [`SearchSort`](/portal-widgets/reference/widget-worker-sdk/functions/SearchSort)
- [`FluidSpacerWidget`](/portal-widgets/reference/widget-worker-sdk/functions/FluidSpacerWidget)

## Capability declarations

Every portal function and declarative capability that a widget calls must appear in that widget's `uses` array. Import and declare the same typed function value instead of copying a capability name into a separate list.

The callable capability exports are under **Variables** because the SDK exports them as typed `const` values. Each reference page documents the input, result, required `uses` declaration, runtime constraints, and error behavior.

## Supported boundary

This reference covers the supported worker-safe exports from `@fluid-app/portal-sdk/widgets/worker`.

<Card title="Widget Package authoring" icon="puzzle-piece" href="/portal-widgets/widget-packages/choose-package-model">
Choose a package model, define widgets, validate them, and publish through the supported lifecycle.
</Card>
